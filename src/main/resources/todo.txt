1.

    validator.validate(operation); // check if all mandatory data exist (fields)

2.

      -  login localhost:8080 -u superuser -p superuser -o organization_1
         > do login

      -  login localhost:8080
         > ask $name and $password

3.

    @Parameter(dependsOn = "login", values = @Value)
    String connectionString;

    @Parameter(values = @Value)
    String username;

    @Parameter(values = @Value)
    String password;

    @Parameter(values = @Value)
    String server;

4.

    ...